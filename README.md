# subfinder RECOMANDED 
# ubuntu
# debian
# COMMAND TO INSTALL 
```
apt-get update && apt-get upgrade -y && wget https://raw.githubusercontent.com/MAHBOUB-MILLON/subfinder/main/subfinder && chmod 777 subfinder
```
```
./subfinder -d isp.com
```
